package com.seekon.system.common.client.category;

import com.seekon.smartclient.component.viewedit.event.EventType;

public enum CategoryEventType implements EventType{
  
  CATEGORY_ADDED,
  
  CATEGORY_UPDATED,
  
  CATEGORY_DELETED,
  
  CATEGORY_SELECTED,
}
